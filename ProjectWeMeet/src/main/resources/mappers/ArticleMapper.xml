<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bsh.projectwemeet.mappers.ArticleMapper">
    <insert id="insertArticle"
            useGeneratedKeys="true"
            keyColumn="index"
            keyProperty="index"
            parameterType="com.bsh.projectwemeet.entities.ArticleEntity">
        INSERT INTO `wemeet_project`.`articles` (email, title, category, content, place, address, thumbnail, thumbnail_mime, view, created_at, day, time, `limit`, participation, like_count, report, deleted_flag, finished_flag, latitude, longitude, hashtag, client_ip, client_ua)
        VALUES (#{email},
                #{title},
                #{category},
                #{content},
                #{place},
                #{address},
                #{thumbnail},
                #{thumbnailMime},
                #{view},
                IFNULL(#{createdAt}, DEFAULT(`created_at`)),
                #{appointmentStartDate},
                #{appointmentStartTime},
                #{limit},
                #{participation},
                #{likeCount},
                #{report},
                #{isDeleted},
                #{isFinished},
                #{latitude},
                #{longitude},
                #{hashtag},
                #{clientIp},
                #{clientUa})
    </insert>
</mapper>